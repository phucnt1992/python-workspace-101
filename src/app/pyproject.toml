
[project]
name = "app"
version = "1.0.0"
description = "FastAPI application with SQLModel"
requires-python = ">=3.13"
dependencies = [
    "fastapi",
    "sqlmodel",
    "pydantic",
    "pydantic-settings",
    "sqlalchemy",
]
