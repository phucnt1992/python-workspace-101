[project]
name = "use_cases"
version = "1.0.0"
description = "Use cases layer for the application"
dependencies = ["domain", "infra"]

[build-system]
requires = ["uv_build>=0.11.14,<0.12"]
build-backend = "uv_build"
